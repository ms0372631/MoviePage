import React, { useState, useEffect } from "react";

export const LikeList = () => {
  const [likeList, useLikeList] = useState();

  useEffect(() => {

  }, [likeList])

  return (
    <>List of LikeLists</>
  )
}